# Coag Reporter (prototype)

An offline, client-only coagulation report generator. Open the page and all data
stays in your browser — nothing is uploaded anywhere. **No patient data is in
this repository.** Decision-support only; the pathologist is the final author.
